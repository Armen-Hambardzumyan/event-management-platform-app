type Event @model @auth(rules: [
  { allow: owner, operations: [update, delete] },
  { allow: public, operations: [read, create] }
]) {
  id: ID!
  name: String!
  description: String!
  date: String!
}
